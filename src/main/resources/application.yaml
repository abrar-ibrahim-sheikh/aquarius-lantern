spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/virgo
    username: root
    password: password
  jpa:
    properties.hibernate: org.hibernate.dialect.MySQL8Dialect
    hibernate.ddl-auto: update
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

project:
  uploads:
    images: aquarius-lantern/uploads/images